# Agent Guidelines

## Build Artifacts
* The name of the release artifact in `.github/workflows/build.yml` has been changed from `kinetic-app-release` to `steadytrend-app-release`. Please ensure any scripts or steps consuming this artifact use the updated name.
